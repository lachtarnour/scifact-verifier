# Web framework
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
gunicorn>=21.2.0

# Retrieval
rank-bm25>=0.2.2
nltk>=3.8.0
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0

# Deep learning (CPU)
torch>=2.2.0
transformers>=4.40.0
accelerate>=0.33.0
peft>=0.12.0
adapters>=1.0.0
wandb>=0.17.0

# Dataset loading
datasets>=2.20.0

# Evaluation
ranx>=0.3.16

# LLM — Ollama (default, via HTTP) needs only requests
requests>=2.31.0

# LLM — Claude API (optional, if ANTHROPIC_API_KEY is set)
anthropic>=0.40.0

# Utilities
numpy>=1.26.0
scikit-learn>=1.4.0
tqdm>=4.66.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
